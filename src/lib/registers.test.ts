import{describe,expect,it}from"vitest";import{collectRegisterData,makeRegisterReference,parseRegisterFields,registerStatusLabel}from"./registers";
describe("register framework",()=>{it("parses configured fields",()=>{expect(parseRegisterFields([{key:"outcome",label:"Outcome",type:"textarea"}])).toHaveLength(1);expect(parseRegisterFields(null)).toEqual([])});it("creates an internal reference",()=>{expect(makeRegisterReference("data-breaches",new Date("2026-07-25T00:00:00Z"),7)).toBe("DB-20260725-007")});it("collects typed data",()=>{const form=new FormData();form.set("field_learning","Shared at team meeting");form.set("field_notified","true");expect(collectRegisterData(form,[{key:"learning",label:"Learning",type:"text"},{key:"notified",label:"Notified",type:"boolean"}])).toEqual({learning:"Shared at team meeting",notified:true})});it("formats status",()=>expect(registerStatusLabel("AWAITING_ACTION")).toBe("Awaiting action"))});
