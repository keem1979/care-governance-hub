"use client";import{useRouter}from"next/navigation";import{useState}from"react";import{ACTION_PRIORITIES}from"@/lib/actions";
export function MeetingArchive({id,archived}:{id:string;archived:boolean}){const router=useRouter(),[busy,setBusy]=useState(false);async function act(){if(!confirm(`${archived?"Restore":"Archive"} this meeting?`))return;setBusy(true);const response=await fetch(`/api/meetings/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({intent:archived?"restore":"archive"})});setBusy(false);if(response.ok)router.refresh()}return <button onClick={act} disabled={busy} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">{busy?"Working…":archived?"Restore":"Archive"}</button>}
export function ExtractActionForm({meetingId,agendaId,title,owners}:{meetingId:string;agendaId:string;title:string;owners:{id:string;name:string}[]}){const router=useRouter(),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const response=await fetch(`/api/meetings/${meetingId}/actions`,{method:"POST",body:new FormData(event.currentTarget)});const result=await response.json().catch(()=>({}));if(!response.ok){setError(result.error??"Could not create action.");setBusy(false);return;}router.refresh()}const cls="rounded-lg border border-slate-300 px-3 py-2 text-sm";return <form onSubmit={submit} className="mt-3 grid gap-2 md:grid-cols-[1fr_180px_150px_150px_auto]"><input type="hidden" name="agendaId" value={agendaId}/><input name="title" defaultValue={title} required className={cls}/><select name="ownerId" required className={cls}><option value="">Owner</option>{owners.map((item)=><option key={item.id} value={item.id}>{item.name}</option>)}</select><select name="priority" className={cls}>{ACTION_PRIORITIES.map((item)=><option key={item}>{item}</option>)}</select><input name="dueDate" type="date" required className={cls}/><button disabled={busy} className="rounded-lg bg-emerald-700 px-3 py-2 text-sm font-semibold text-white">{busy?"Creating…":"Create action"}</button>{error&&<p className="text-sm text-red-700 md:col-span-5">{error}</p>}</form>}
