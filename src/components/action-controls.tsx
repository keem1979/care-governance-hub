"use client";
import{useRouter}from"next/navigation";import{useState}from"react";import{actionLabel}from"@/lib/actions";
export function ActionArchive({id,archived}:{id:string;archived:boolean}){const router=useRouter();const[busy,setBusy]=useState(false);async function act(){if(!confirm(`${archived?"Restore":"Archive"} this action?`))return;setBusy(true);const response=await fetch(`/api/actions/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({intent:archived?"restore":"archive"})});setBusy(false);if(response.ok)router.refresh();}return <button onClick={act} disabled={busy} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold disabled:opacity-60">{busy?"Working…":archived?"Restore":"Archive"}</button>}
export function ActionUpdateForm({id,status}:{id:string;status:string}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const response=await fetch(`/api/actions/${id}/updates`,{method:"POST",body:new FormData(event.currentTarget)});const result=await response.json().catch(()=>({}));if(!response.ok){setError(result.error??"Could not add update.");setBusy(false);return;}(event.currentTarget as HTMLFormElement).reset();setBusy(false);router.refresh();}const cls="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2";return <form onSubmit={submit} className="grid gap-4 md:grid-cols-[1fr_220px_auto] md:items-end">{error&&<p className="md:col-span-3 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="text-sm font-medium">Progress update<textarea name="note" required minLength={3} className={`${cls} min-h-20`}/></label><label className="text-sm font-medium">Progress status<select name="status" defaultValue={["OPEN","OVERDUE"].includes(status)?"IN_PROGRESS":status} className={cls}>{["IN_PROGRESS","AWAITING_EVIDENCE","AWAITING_VERIFICATION"].map((item)=><option key={item} value={item}>{actionLabel(item)}</option>)}</select></label><button disabled={busy} className="rounded-lg bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{busy?"Saving…":"Add update"}</button></form>}
