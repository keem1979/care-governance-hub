"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export function PublishCarePlan({id,versionId,defaultDecision}:{id:string;versionId:string;defaultDecision:string}){const router=useRouter(),[busy,setBusy]=useState(false),[error,setError]=useState("");async function publish(){setBusy(true);setError("");const form=new FormData();form.set("versionId",versionId);form.set("decision",defaultDecision);const response=await fetch(`/api/care-plans/${id}/publish`,{method:"POST",body:form});const body=await response.json().catch(()=>({}));if(!response.ok){setError(body.error??"Could not publish.");setBusy(false);return;}router.refresh();}return <div>{error?<p role="alert" className="mb-2 max-w-xl rounded-lg bg-red-50 p-2 text-xs font-semibold text-red-700">{error}</p>:null}<button type="button" disabled={busy} onClick={publish} className="rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{busy?"Publishing…":"Approve and publish"}</button></div>}

export function AcknowledgeCarePlan({id,staff}:{id:string;staff:{id:string;name:string}[]}){const router=useRouter(),[staffId,setStaffId]=useState(""),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");async function acknowledge(){setBusy(true);setError("");const form=new FormData();if(staffId)form.set("staffMemberId",staffId);const response=await fetch(`/api/care-plans/${id}/acknowledge`,{method:"POST",body:form});const body=await response.json().catch(()=>({}));if(!response.ok){setError(body.error??"Could not acknowledge.");setBusy(false);return;}setDone(true);setBusy(false);router.refresh();}return <div className="rounded-2xl border border-amber-300 bg-amber-50 p-4"><h3 className="font-bold text-amber-950">Read & Understood</h3><p className="mt-1 text-sm text-amber-900">Confirm that you have read the current version and will follow its authorised instructions.</p>{staff.length?<select value={staffId} onChange={(e)=>setStaffId(e.target.value)} className="mt-3 rounded-lg border bg-white p-2 text-sm"><option value="">Your linked workforce record (optional)</option>{staff.map((item)=><option key={item.id} value={item.id}>{item.name}</option>)}</select>:null}{error?<p className="mt-2 text-xs font-semibold text-red-700">{error}</p>:null}<button type="button" onClick={acknowledge} disabled={busy||done} className="ml-2 mt-3 rounded-lg bg-amber-900 px-3 py-2 text-sm font-bold text-white disabled:opacity-50">{done?"Acknowledged":busy?"Recording…":"Confirm understood"}</button></div>}
