"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function AuditActions({id,status}:{id:string;status:string}){const router=useRouter();const [error,setError]=useState("");async function act(intent:string){const form=new FormData();form.set("intent",intent);const response=await fetch(`/api/audits/${id}`,{method:"PATCH",body:form});if(!response.ok){const result=await response.json();setError(result.error??"Action failed.");return;}router.refresh();}return <div><div className="flex flex-wrap gap-2">{status==="AWAITING_REVIEW"&&<button onClick={()=>act("complete")} className="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Complete and sign off</button>}{status==="COMPLETED"&&<button onClick={()=>act("close")} className="rounded-lg bg-slate-800 px-4 py-2 text-sm font-semibold text-white">Close audit</button>}<button onClick={()=>act(status==="ARCHIVED"?"restore":"archive")} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">{status==="ARCHIVED"?"Restore":"Archive"}</button></div>{error&&<p className="mt-2 text-sm text-red-700">{error}</p>}</div>}
