"use client";import{useRouter}from"next/navigation";import{useState}from"react";export function RegisterEntryActions({registerKey,id,archived}:{registerKey:string;id:string;archived:boolean}){const router=useRouter();const[error,setError]=useState("");async function act(){const form=new FormData();form.set("intent",archived?"restore":"archive");const response=await fetch(`/api/registers/${registerKey}/${id}`,{method:"PATCH",body:form});if(!response.ok){const result=await response.json();setError(result.error??"Action failed.");return;}router.refresh();}return <div><button onClick={act} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">{archived?"Restore":"Archive"}</button>{error&&<p className="mt-2 text-sm text-red-700">{error}</p>}</div>}
