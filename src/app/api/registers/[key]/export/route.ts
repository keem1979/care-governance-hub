import{requirePermission}from"@/lib/auth/dal";import{createDb}from"@/lib/db";import{PERMISSIONS}from"@/lib/permissions";import{registerScopeWhere}from"@/lib/registers";import{safeDownloadName}from"@/lib/policies";
const escapeCsv=(value:unknown)=>`"${String(value??"").replaceAll('"','""')}"`;const xml=(value:unknown)=>String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");
export async function GET(request:Request,{params}:{params:Promise<{key:string}>}){const context=await requirePermission(PERMISSIONS.REPORTS_EXPORT);const{key}=await params;const format=new URL(request.url).searchParams.get("format")==="xls"?"xls":"csv";const db=createDb();const definition=await db.registerDefinition.findFirst({where:{key,isPublished:true,OR:[{organisationId:null},{organisationId:context.organisation.id}]}});if(!definition){await db.$disconnect();return new Response("Not found",{status:404});}const entries=await db.registerEntry.findMany({where:{...registerScopeWhere(context),definitionId:definition.id},include:{location:{select:{name:true}},owner:{select:{name:true}},_count:{select:{evidenceLinks:true}}},orderBy:{eventDate:"desc"}}).finally(()=>db.$disconnect());const header=["Reference","Date","Title","Summary","Risk","Status","Location","Owner","Evidence"];const rows=entries.map((item)=>[item.reference,item.eventDate.toISOString().slice(0,10),item.title,item.summary,item.riskLevel,item.status,item.location?.name??"Organisation-wide",item.owner?.name??"",item._count.evidenceLinks]);if(format==="xls"){const sheet=`<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet"><Worksheet ss:Name="${xml(definition.name)}" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Table>${[header,...rows].map((row)=>`<Row>${row.map((cell)=>`<Cell><Data ss:Type="String">${xml(cell)}</Data></Cell>`).join("")}</Row>`).join("")}</Table></Worksheet></Workbook>`;return new Response(sheet,{headers:{"Content-Type":"application/vnd.ms-excel","Content-Disposition":`attachment; filename="${safeDownloadName(key)}-register.xls"`}});}return new Response(`\uFEFF${[header,...rows].map((row)=>row.map(escapeCsv).join(",")).join("\r\n")}`,{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":`attachment; filename="${safeDownloadName(key)}-register.csv"`}});}
