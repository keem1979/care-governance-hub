import{NextResponse}from"next/server";import{requirePermission}from"@/lib/auth/dal";import{createDb}from"@/lib/db";import{PERMISSIONS}from"@/lib/permissions";import{deletePrivateFile,getPrivateFile,putPrivateFile}from"@/lib/private-storage";import{templateEvidenceCategory,templateScopeWhere}from"@/lib/templates";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const context=await requirePermission(PERMISSIONS.EVIDENCE_UPLOAD),{id}=await params,form=await request.formData(),ownerId=String(form.get("ownerId")??""),locationId=String(form.get("locationId")??"")||null,db=createDb();let storageKey:string|null=null;try{if(locationId&&!context.locations.some((item)=>item.id===locationId))throw new Error("Choose an authorised location.");if(!(await db.organisationMembership.findFirst({where:{organisationId:context.organisation.id,userId:ownerId,status:"ACTIVE"}})))throw new Error("Choose an active evidence owner.");const template=await db.template.findFirst({where:{id,...templateScopeWhere(context.organisation.id),status:{not:"ARCHIVED"}}});if(!template)throw new Error("Template not found.");const source=template.storageKey?await getPrivateFile(template.storageKey):template.bodyText;if(source===null)throw new Error("Template file is unavailable.");const bytes=await new Response(source).arrayBuffer();storageKey=`${context.organisation.id}/evidence/${crypto.randomUUID()}`;await putPrivateFile(storageKey,bytes);const evidence=await db.$transaction(async(tx)=>{const created=await tx.evidence.create({data:{organisationId:context.organisation.id,locationId,title:`${template.title} - working copy`,description:`Copied from Template Library: ${template.description}`,category:templateEvidenceCategory(template.category),evidenceType:"Form",ownerId,tags:[...template.tags,"template-copy"],relatedModule:"Template",relatedRecordId:template.id,confidentiality:"INTERNAL",notes:`Based on ${template.title} version ${template.version}.`,uploadedById:context.user.id}});const version=await tx.evidenceVersion.create({data:{evidenceId:created.id,versionNumber:"1.0",storageKey:storageKey!,fileName:template.fileName,contentType:template.contentType,sizeBytes:bytes.byteLength,checksum:await hash(bytes),changeNotes:`Copied from template version ${template.version}`,uploadedById:context.user.id}});await tx.evidence.update({where:{id:created.id},data:{currentVersionId:version.id}});await tx.activityLog.create({data:{organisationId:context.organisation.id,locationId,userId:context.user.id,action:"CREATE",recordType:"Evidence",recordId:created.id,summary:`Copied template into Evidence Library: ${template.title}`,afterValue:{templateId:template.id,templateVersion:template.version}}});return created});return NextResponse.json({id:evidence.id},{status:201})}catch(error){if(storageKey)await deletePrivateFile(storageKey);return NextResponse.json({error:error instanceof Error?error.message:"Could not copy template."},{status:400})}finally{await db.$disconnect()}}
async function hash(bytes:ArrayBuffer){return Array.from(new Uint8Array(await crypto.subtle.digest("SHA-256",bytes))).map((byte)=>byte.toString(16).padStart(2,"0")).join("")}
