import { notFound } from "next/navigation";import {CarePlanView}from"@/components/care-plan-view";import{requirePermission}from"@/lib/auth/dal";import{loadCarePlan}from"@/lib/care-plan-data";import{createDb}from"@/lib/db";import{PERMISSIONS,ROLE_KEYS}from"@/lib/permissions";
export default async function CarePlanReport({params}:{params:Promise<{id:string}>}){const context=await requirePermission(PERMISSIONS.REPORTS_EXPORT),{id}=await params,db=createDb();try{const x=await loadCarePlan(db,context,id);if(!x)notFound();const canViewRestricted=new Set<string>([ROLE_KEYS.OWNER,ROLE_KEYS.REGISTERED_MANAGER,ROLE_KEYS.QUALITY_MANAGER]).has(context.role.key);return <main className="bg-white p-8 print:p-0"><p className="mb-4 rounded-lg bg-emerald-700 p-2 text-center text-sm font-bold text-white print:hidden">Use Print to create the organisation-branded A4 PDF.</p><CarePlanView plan={x.plan} version={x.current??x.display} person={x.person} organisation={{name:context.organisation.name,hasLogo:Boolean(context.organisation.policyLogoStorageKey)}} location={x.location} coordinator={x.coordinator} manager={x.manager} actions={x.actions} evidence={x.evidence} acknowledgements={x.acknowledgements} canViewRestricted={canViewRestricted} print/></main>}finally{await db.$disconnect()}}
