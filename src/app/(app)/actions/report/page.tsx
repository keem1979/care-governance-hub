import{requirePermission}from"@/lib/auth/dal";import{actionLabel,actionScopeWhere,effectiveActionStatus}from"@/lib/actions";import{createDb}from"@/lib/db";import{PERMISSIONS}from"@/lib/permissions";
export default async function ActionReportPage(){const context=await requirePermission(PERMISSIONS.REPORTS_EXPORT),db=createDb();try{const actions=await db.action.findMany({where:actionScopeWhere(context),include:{owner:{select:{name:true}},location:{select:{name:true}},_count:{select:{evidenceLinks:true}}},orderBy:[{dueDate:"asc"},{priority:"desc"}]});return <main className="mx-auto max-w-6xl bg-white p-8 text-slate-900 print:p-0"><header className="flex items-start justify-between border-b-4 border-emerald-800 pb-5"><div><p className="font-bold uppercase tracking-widest text-emerald-800">{context.organisation.name}</p><h1 className="mt-2 text-4xl font-bold">Action Tracker</h1><p className="mt-1 text-slate-600">Accountable improvement and evidence-based closure.</p></div><p className="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white print:hidden">Press Ctrl+P to save PDF</p></header><p className="my-5 text-sm text-slate-500">{actions.length} actions · Generated {new Date().toLocaleDateString("en-GB")}</p><table className="w-full border-collapse text-left text-xs"><thead><tr className="bg-slate-100"><th className="border p-2">Reference</th><th className="border p-2">Action</th><th className="border p-2">Source</th><th className="border p-2">Priority</th><th className="border p-2">Status</th><th className="border p-2">Owner</th><th className="border p-2">Due</th><th className="border p-2">Evidence</th></tr></thead><tbody>{actions.map((action)=><tr key={action.id} className="break-inside-avoid"><td className="border p-2 font-mono">{action.reference}</td><td className="border p-2"><strong>{action.title}</strong><br/>{action.description}</td><td className="border p-2">{actionLabel(action.sourceType)}<br/>{action.sourceReference??""}</td><td className="border p-2">{action.priority}</td><td className="border p-2">{actionLabel(effectiveActionStatus(action.status,action.dueDate))}</td><td className="border p-2">{action.owner.name}</td><td className="border p-2">{date(action.dueDate)}</td><td className="border p-2">{action._count.evidenceLinks}</td></tr>)}</tbody></table><footer className="mt-8 border-t pt-3 text-xs text-slate-500">Care Governance Hub · Internal governance record</footer></main>}finally{await db.$disconnect()}}function date(value:Date){return new Intl.DateTimeFormat("en-GB",{dateStyle:"medium"}).format(value)}
