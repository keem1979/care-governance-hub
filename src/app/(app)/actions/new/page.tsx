import Link from"next/link";import{ActionForm}from"@/components/action-form";import{requirePermission}from"@/lib/auth/dal";import{listActionSources}from"@/lib/action-sources";import{createDb}from"@/lib/db";import{evidenceScopeWhere}from"@/lib/evidence";import{PERMISSIONS}from"@/lib/permissions";
export default async function NewActionPage({searchParams}:{searchParams:Promise<{sourceType?:string;sourceId?:string}>}){const context=await requirePermission(PERMISSIONS.ACTIONS_MANAGE),query=await searchParams,db=createDb();try{const[memberships,evidence,sources]=await Promise.all([db.organisationMembership.findMany({where:{organisationId:context.organisation.id,status:"ACTIVE"},select:{user:{select:{id:true,name:true}}},orderBy:{user:{name:"asc"}}}),db.evidence.findMany({where:{...evidenceScopeWhere(context),status:"ACTIVE"},select:{id:true,title:true},orderBy:{title:"asc"}}),listActionSources(db,context)]);const requested=query.sourceType&&query.sourceId?`${query.sourceType}:${query.sourceId}`:undefined,preselected=requested&&sources.some((item)=>`${item.type}:${item.id}`===requested)?requested:undefined;return <main className="mx-auto max-w-5xl space-y-5"><div><Link href="/actions" className="text-sm font-semibold text-emerald-700">← Action Tracker</Link><h1 className="mt-2 text-3xl font-bold">Create improvement action</h1><p className="mt-1 text-slate-600">Create one accountable record that remains connected to its source, evidence, calendar and reports.</p></div><ActionForm locations={context.locations.map(({id,name})=>({id,name}))} owners={memberships.map(({user})=>user)} evidence={evidence.map(({id,title})=>({id,name:title}))} sources={sources} preselectedSource={preselected}/></main>}finally{await db.$disconnect()}}
