import{notFound}from"next/navigation";import{requirePermission}from"@/lib/auth/dal";import{createDb}from"@/lib/db";import{meetingLabel,meetingScopeWhere}from"@/lib/meetings";import{PERMISSIONS}from"@/lib/permissions";
export default async function MinutesPage({params}:{params:Promise<{id:string}>}){const context=await requirePermission(PERMISSIONS.REPORTS_EXPORT),{id}=await params,db=createDb();try{const meeting=await db.governanceMeeting.findFirst({where:{id,...meetingScopeWhere(context)},include:{chair:{select:{name:true}},approvedBy:{select:{name:true}},attendees:{include:{user:{select:{name:true}}}},agendaItems:{orderBy:{sortOrder:"asc"}}}});if(!meeting)notFound();const actions=await db.action.findMany({where:{organisationId:context.organisation.id,sourceType:"GOVERNANCE_MEETING",sourceRecordId:id},include:{owner:{select:{name:true}}},orderBy:{dueDate:"asc"}});return <main className="mx-auto max-w-4xl bg-white p-8 print:p-0"><header className="border-b-4 border-emerald-800 pb-5"><p className="font-bold uppercase tracking-widest text-emerald-800">{context.organisation.name}</p><h1 className="mt-2 text-4xl font-bold">Meeting minutes</h1><h2 className="mt-2 text-2xl">{meeting.title}</h2><p>{date(meeting.meetingDate)} · {meeting.meetingTime} · {meeting.locationOrLink}</p><p className="mt-3 w-fit rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white print:hidden">Press Ctrl+P to save PDF</p></header><section className="mt-6 text-sm"><p><strong>Chair:</strong> {meeting.chair.name}</p><p><strong>Attendees:</strong> {meeting.attendees.filter((item)=>item.attendance!=="APOLOGY").map((item)=>item.user.name).join(", ")||"None"}</p><p><strong>Apologies:</strong> {meeting.attendees.filter((item)=>item.attendance==="APOLOGY").map((item)=>item.user.name).join(", ")||"None"}</p></section><section className="mt-6"><h2 className="text-xl font-bold">Agenda discussion and decisions</h2><ol className="mt-3 space-y-5">{meeting.agendaItems.map((item)=><li key={item.id} className="break-inside-avoid border-b pb-4"><p className="text-xs font-bold uppercase text-emerald-800">{item.sortOrder}. {item.topic}</p><h3 className="font-bold">{item.title}</h3><p className="mt-1 whitespace-pre-wrap text-sm">{item.notes??"No discussion recorded."}</p><p className="mt-2 text-sm"><strong>Decision:</strong> {item.decision??"No decision recorded."}</p></li>)}</ol></section><section className="mt-6"><h2 className="text-xl font-bold">Formal minutes</h2><p className="mt-2 whitespace-pre-wrap text-sm">{meeting.minutes??"Minutes not yet recorded."}</p></section>{actions.length>0&&<section className="mt-6"><h2 className="text-xl font-bold">New actions</h2><table className="mt-2 w-full text-left text-xs"><thead><tr><th>Reference</th><th>Action</th><th>Owner</th><th>Due</th></tr></thead><tbody>{actions.map((action)=><tr key={action.id} className="border-t"><td className="py-2">{action.reference}</td><td>{action.title}</td><td>{action.owner.name}</td><td>{date(action.dueDate)}</td></tr>)}</tbody></table></section>}<footer className="mt-8 border-t pt-3 text-xs"><p>Status: {meetingLabel(meeting.status)} · Approved by {meeting.approvedBy?.name??"Not approved"} · {meeting.approvalDate?date(meeting.approvalDate):"No approval date"}</p></footer></main>}finally{await db.$disconnect()}}function date(value:Date){return new Intl.DateTimeFormat("en-GB",{dateStyle:"long"}).format(value)}
