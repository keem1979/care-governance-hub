import Link from"next/link";import{notFound}from"next/navigation";import{RegisterEntryForm}from"@/components/register-entry-form";import{requirePermission}from"@/lib/auth/dal";import{createDb}from"@/lib/db";import{evidenceScopeWhere}from"@/lib/evidence";import{PERMISSIONS}from"@/lib/permissions";import{parseRegisterFields,registerScopeWhere}from"@/lib/registers";
const dateInput=(value:Date|null)=>value?.toISOString().slice(0,10)??"";
export default async function EditRegisterEntryPage({params}:{params:Promise<{key:string;id:string}>}){const context=await requirePermission(PERMISSIONS.GOVERNANCE_EDIT);const{key,id}=await params;const db=createDb();try{const[entry,memberships,evidence]=await Promise.all([db.registerEntry.findFirst({where:{id,...registerScopeWhere(context),definition:{key}},include:{definition:true,evidenceLinks:true}}),db.organisationMembership.findMany({where:{organisationId:context.organisation.id,status:"ACTIVE"},select:{user:{select:{id:true,name:true}}},orderBy:{user:{name:"asc"}}}),db.evidence.findMany({where:{...evidenceScopeWhere(context),status:"ACTIVE"},select:{id:true,title:true},orderBy:{title:"asc"}})]);if(!entry)notFound();return <main className="mx-auto max-w-4xl space-y-5"><div><Link href={`/registers/${key}/${id}`} className="text-sm font-semibold text-emerald-700">← Back to entry</Link><h1 className="mt-2 text-3xl font-bold">Edit {entry.definition.name} entry</h1></div><RegisterEntryForm registerKey={key} registerName={entry.definition.name} fields={parseRegisterFields(entry.definition.fieldSchema)} locations={context.locations.map((item)=>({id:item.id,name:item.name}))} owners={memberships.map(({user})=>user)} evidence={evidence} initial={{id:entry.id,reference:entry.reference,eventDate:dateInput(entry.eventDate),title:entry.title,summary:entry.summary,riskLevel:entry.riskLevel,status:entry.status,locationId:entry.locationId??"",ownerId:entry.ownerId??"",closureDate:dateInput(entry.closureDate),data:entry.data as Record<string,unknown>,evidenceIds:entry.evidenceLinks.map((item)=>item.evidenceId)}}/></main>}finally{await db.$disconnect()}}
