import Link from"next/link";import{requirePermission}from"@/lib/auth/dal";import{createDb}from"@/lib/db";import{PERMISSIONS}from"@/lib/permissions";import{registerScopeWhere}from"@/lib/registers";
export default async function RegistersPage(){const context=await requirePermission(PERMISSIONS.GOVERNANCE_VIEW);const db=createDb();const[definitions,counts]=await Promise.all([db.registerDefinition.findMany({where:{isPublished:true,OR:[{organisationId:null},{organisationId:context.organisation.id}]},orderBy:{sortOrder:"asc"}}),db.registerEntry.groupBy({by:["definitionId"],where:registerScopeWhere(context),_count:{_all:true}})]).finally(()=>db.$disconnect());const countMap=new Map(counts.map((item)=>[item.definitionId,item._count._all]));return <main className="space-y-7"><div><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">Operational oversight</p><h1 className="text-3xl font-bold">Registers</h1><p className="mt-1 text-slate-600">Consistent records, evidence, learning and follow-up across your service.</p></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{definitions.map((definition)=><Link key={definition.id} href={`/registers/${definition.key}`} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-emerald-400"><div className="flex items-start justify-between gap-3"><h2 className="text-lg font-bold">{definition.name}</h2><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-800">{countMap.get(definition.id)??0}</span></div><p className="mt-2 text-sm leading-6 text-slate-600">{definition.description}</p><p className="mt-4 text-sm font-semibold text-emerald-700">Open register →</p></Link>)}</section></main>}
