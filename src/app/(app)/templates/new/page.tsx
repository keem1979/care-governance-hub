import Link from"next/link";import{TemplateForm}from"@/components/template-controls";import{requirePermission}from"@/lib/auth/dal";import{createDb}from"@/lib/db";import{PERMISSIONS}from"@/lib/permissions";
export default async function NewTemplatePage(){const context=await requirePermission(PERMISSIONS.GOVERNANCE_EDIT),db=createDb();try{const members=await db.organisationMembership.findMany({where:{organisationId:context.organisation.id,status:"ACTIVE"},select:{user:{select:{id:true,name:true}}},orderBy:{user:{name:"asc"}}});return <main className="mx-auto max-w-4xl space-y-5"><div><Link href="/templates" className="text-sm font-semibold text-emerald-700">Back to Template Library</Link><h1 className="mt-2 text-3xl font-bold">Add organisation template</h1><p className="mt-1 text-slate-600">Upload a controlled reusable resource for your organisation.</p></div><TemplateForm members={members.map(({user})=>user)}/></main>}finally{await db.$disconnect()}}
