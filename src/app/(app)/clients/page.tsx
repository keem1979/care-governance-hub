import Link from "next/link";
import { requirePermission } from "@/lib/auth/dal";
import { CLIENT_STATUSES, clientLabel, clientName, clientScopeWhere } from "@/lib/clients";
import { createDb } from "@/lib/db";
import { hasPermission, PERMISSIONS } from "@/lib/permissions";

export default async function ClientsPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const context=await requirePermission(PERMISSIONS.GOVERNANCE_VIEW),query=await searchParams,q=String(query.q??"").trim(),status=String(query.status??""),db=createDb();try{const base=clientScopeWhere(context);const clients=await db.client.findMany({where:{AND:[base,...(q?[{OR:[{clientReference:{contains:q,mode:"insensitive" as const}},{firstName:{contains:q,mode:"insensitive" as const}},{lastName:{contains:q,mode:"insensitive" as const}},{preferredName:{contains:q,mode:"insensitive" as const}}]}]:[])],...(status?{status:status as never}:{})},include:{location:{select:{name:true}},_count:{select:{registerEntries:true}}},orderBy:[{lastName:"asc"},{firstName:"asc"}],take:300});const canEdit=hasPermission(context.permissions,PERMISSIONS.GOVERNANCE_EDIT);return <main className="space-y-7"><header className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-widest text-emerald-700">People receiving support</p><h1 className="text-3xl font-bold">Client Directory</h1><p className="mt-1 max-w-3xl text-slate-600">Find a person once, then open their assessments, reviews, incidents and linked evidence from one profile.</p></div>{canEdit?<Link href="/clients/new" className="rounded-xl bg-emerald-700 px-5 py-3 text-sm font-semibold text-white">Add client</Link>:null}</header><form className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 md:grid-cols-[2fr_1fr_auto]"><input name="q" defaultValue={q} placeholder="Search name or client reference" className="rounded-lg border border-slate-300 px-3 py-2 text-sm"/><select name="status" defaultValue={status} className="rounded-lg border border-slate-300 px-3 py-2 text-sm"><option value="">All client statuses</option>{CLIENT_STATUSES.map(x=><option key={x} value={x}>{clientLabel(x)}</option>)}</select><button className="rounded-lg bg-slate-800 px-4 py-2 text-sm font-semibold text-white">Search</button></form>{clients.length?<section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Person</th><th className="px-4 py-3">Service</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Linked records</th></tr></thead><tbody className="divide-y divide-slate-100">{clients.map(person=><tr key={person.id}><td className="px-4 py-4"><Link href={`/clients/${person.id}`} className="font-bold text-emerald-800 hover:underline">{clientName(person)}</Link><p className="font-mono text-xs text-slate-500">{person.clientReference}</p></td><td className="px-4 py-4">{person.location?.name??"Organisation-wide"}</td><td className="px-4 py-4">{clientLabel(person.status)}</td><td className="px-4 py-4 font-semibold">{person._count.registerEntries}</td></tr>)}</tbody></table></section>:<section className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><h2 className="font-bold">No client records found</h2><p className="mt-1 text-sm text-slate-600">Add the first client or change your search.</p></section>}</main>}finally{await db.$disconnect()}}
