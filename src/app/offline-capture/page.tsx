import Link from "next/link";
import { LockKeyhole, ShieldAlert } from "lucide-react";
import { OfflineCaptureTool } from "@/components/offline-capture";

export const metadata = { title: "Encrypted offline capture | QCGMS", description: "Create an encrypted device-local governance observation and synchronise it for authorised review." };

export default function OfflineCapturePage() { return <main className="mx-auto min-h-screen max-w-6xl space-y-7 bg-slate-50 px-4 py-8 text-slate-950 sm:px-6"><header className="rounded-3xl bg-gradient-to-br from-slate-950 via-teal-950 to-emerald-900 p-7 text-white"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-sm font-black uppercase tracking-[.18em] text-emerald-300">QCGMS · encrypted offline capture</p><h1 className="mt-2 max-w-3xl text-3xl font-black">Record safely when the connection is unavailable</h1><p className="mt-3 max-w-3xl text-emerald-50">Content is encrypted before it is stored in this browser. Synchronised captures enter a controlled review queue and never overwrite a source record automatically.</p></div><LockKeyhole className="size-12 text-emerald-300"/></div><Link href="/login?returnTo=%2Fconnected-governance" className="mt-5 inline-block rounded-xl bg-white/10 px-4 py-2 text-sm font-bold">Return to QCGMS</Link></header><section className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950"><div className="flex gap-3"><ShieldAlert className="mt-0.5 shrink-0"/><p><strong>Not for emergencies:</strong> do not rely on this queue for urgent safeguarding, clinical, medication or immediate safety action. Follow the service’s escalation procedure first.</p></div></section><OfflineCaptureTool/></main>; }
